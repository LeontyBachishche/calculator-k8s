# Распределенный микросервисный калькулятор в Kubernetes

Высокодоступное и отказоустойчивое веб-приложение для вычисления математических выражений, развернутое в кластере **Kubernetes (Minikube)**. Проект демонстрирует применение практик DevOps, безопасное управление секретами, асинхронную обработку задач и комплексный мониторинг.

## 🏗 Архитектура системы

Проект построен на базе микросервисной архитектуры и включает в себя следующие компоненты:

1. **Входной балансировщик (HAProxy)** — принимает внешний трафик на порту `30080` (NodePort) и балансирует HTTP-запросы между репликами веб-приложения.
2. **Фронтенд / Веб-приложение (Flask)** — легковесный веб-интерфейс. Проверяет наличие готового ответа в кэше, отправляет новые задачи в очередь и отдаёт метрики для Prometheus.
3. **Брокер сообщений (RabbitMQ)** — выступает в качестве буфера. Хранит асинхронную очередь задач `calc_tasks`, гарантируя доставку сообщений.
4. **Фоновый обработчик (Python Worker)** — изолированный сервис вычислений. Безопасно парсит выражения, кеширует результаты и инициирует запись в БД.
5. **Кэш (Redis)** — обеспечивает мгновенный доступ к ранее вычисленным выражениям (TTL кэша — 1 час).
6. **База данных (PostgreSQL)** — персистентное хранилище истории зарегистрированных пользователей и успешно выполненных вычислений.
7. **Мониторинг (Prometheus & Grafana)** — сбор кастомных бизнес-метрик приложения и визуализация состояния кластера.

### 🔄 Поток данных (Data Flow)
Пользователь ➔ HAProxy ➔ Flask App ➔ [Проверка Redis (Кэш)]
                         ➔ (Если нет в кэше) ➔ Очередь RabbitMQ ➔ Python Worker ➔ [Вычисление] ➔ Запись в Redis & PostgreSQL

---

## 🔒 Безопасность (Управление секретами)

Все конфиденциальные данные (пароли к базам данных и брокеру сообщений) полностью изолированы от исходного кода. 
* Внутри контейнеров параметры инициализируются динамически через переменные окружения `os.getenv()`.
* Внутри кластера Kubernetes значения безопасно поставляются из манифеста `Secret` (компонент `calc-secrets`).
* Файлы конфигурации секретов и локальные окружения внесены в `.gitignore` и защищены от утечки в публичный репозиторий.

---

## 🚀 Быстрый запуск в Minikube

### 1. Подготовка окружения и сборка образов
Переключите контекст терминала на Docker-демон внутри Minikube и соберите актуальные образы приложений:

```bash
# Активация окружения Minikube
eval \$(minikube docker-env)

# Сборка компонентов
docker build -t calc-web-app:latest ./web-app
docker build -t calc-worker:latest ./worker
```

### 2. Развертывание инфраструктуры и секретов

1. Закодируйте ваши пароли в формат Base64:
   ```bash
   echo -n 'your_password' | base64
   ```
2. Создайте локальный файл `k8s/secrets.yaml` на основе ваших Base64-строк:
   ```yaml
   apiVersion: v1
   kind: Secret
   metadata:
     name: calc-secrets
   type: Opaque
   data:
     postgres-password: <ВАШ_BASE64_ПАРОЛЬ>
     rabbitmq-password: <ВАШ_BASE64_ПАРОЛЬ>
     redis-password: ""
   ```
3. Примените манифесты в кластер:
   ```bash
   # Развертывание секретов и баз данных
   kubectl apply -f k8s/secrets.yaml
   kubectl apply -f k8s/postgres.yaml
   kubectl apply -f k8s/redis.yaml
   kubectl apply -f k8s/rabbitmq.yaml

   # Развертывание приложений и балансировщика
   kubectl apply -f k8s/apps.yaml
   kubectl apply -f k8s/haproxy.yaml
   ```

### 3. Доступ к приложению
После перевода всех подов в статус `Running`, калькулятор будет доступен по адресу:
```bash
minikube ip
```
Используйте полученный IP-адрес совместно с портом балансировщика HAProxy: `http://<MINIKUBE_IP>:30080`.

---

## 📊 Мониторинг и метрики

* Эндпоинт метрик приложения: `http://<MINIKUBE_IP>:30080/metrics`
* Собранные кастомные метрики: `calc_requests_total` (счетчик общего количества запросов с разделением по эндпоинтам `/register` и `/calculate`).
